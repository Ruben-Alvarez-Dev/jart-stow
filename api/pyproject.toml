[project]
name = "jart-stow-api"
version = "0.1.0"
description = "FastAPI backend for Jart-Stow"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0,<1.0.0",
    "uvicorn>=0.30.0,<1.0.0",
    "aiosqlite>=0.20.0,<1.0.0",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.pytest.ini_options]
testpaths = ["tests"]
